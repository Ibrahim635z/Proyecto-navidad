## Proyecto de tienda online



**primeros pasos**:
* Inicializar con GIT.
* Preparar el Entorno (instalar node.js): ```npm init -y```
* Instalar libreria que actuara como API: ``` npm install json-server```
* Creacion de db.json  para almacenar la base de datos.
* En Package.json añadimos el script server para levantar el json server de forma mas automatizada.
* Ejecutamos ese script: ```npm run server```